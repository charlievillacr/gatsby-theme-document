---
title: 'Contents Index'
description: 'This is the meta description'
---

## JavaScript Intro Examples

[Section URL](https://www.w3schools.com/js/js_examples.asp)

### What Can JS do

```markdown
...
1. Change HTML content
2. Change HTML attributes
3. Change CSS style
4. Hide HTML elements
5. Hidden HTML elements
...
```

### Where to Insert JS

```markdown
...
6. In `<head>`
7. In `<body>`
8. In an external file
9. In an external url
10. In an external folder
...
```

### JS Output

```markdown
...
11. Writing into the HTML output
12. Writing into an HTML element
13. Writing into an window alert box
14. Writing into the browser console
...
```

### JS Syntax

```markdown
...
15. Statements
16. Numbers
17. Strings
18. Variables
19. Operators
20. Assignment
21. Expressions (using constants)
22. Expressions (using strings)
23. Expressions (using variables)
24. Keywords
25. Comments
26. Case sensitive
...
```

### JS Statements

```markdown
...
27. JS statements are commands to the browser
28. JS code is a sequence of statements
29. JS statements are separated with semicolon
30. Multiple statement on one line is allowed
31. JS statements ca be grouped together in code blocks
32. You can break a code line after an operator or a comma.
...
```

### JS Comments

```markdown
...
33. Single line comments
34. Single line comments at the end of a line
35. Multiple line comments
36. Single line comment to prevent execution
37. Multiple lines comment to prevent execution
...
```

### JS Variables

```markdown
...
38. JS Variables
39. Variables as algebra
40. JS numbers and strings
41. JS var keyword
42. Declaring many variables in one statement
43. Declaring many variables multiline
44. A variable without a value returns the value undefined
45. Re-declaring a variable will not destroy the value
46. Adding JS numbers
47. Adding JS strings
48. Adding strings and numbers
...
```

### JS Arithmetic

```markdown
...
49. Addition `(+)` operator
50. Subtraction `(-)` operator
51. Multiplication `(*)` operator
52. Modulus `(%)` operator
53. Increment `(++)` operator
54. Decrement `(--)` operator
...
```

### JS Assignment

```markdown
...
55. `=` assigment operator
56. `+=` assignment operator
57. `-=` assignment operator
58. `*=` assigment operator
59. `/=` assigment operator
60. `%=` assignment operator
...
```

### JS String Concatenation

```markdown
...
61. Adding two strings together using the concatenating `(+)` operator
62. Adding two strings together with a space in the first string
63. Adding two string together with a space in between
64. Adding two strings together using the `(+=)` operator
65. Adding strings and numbers
...
```

### JS Data Types

```markdown
...
66. Declare (create) strings
67. Declare (create) numbers
68. Declare (create) an array
69. Declare (create) an object
70. Find the type of a variable
71. Adding two numbers and a string
72. Adding a string and two numbers
73. An undefined variable
74. An empty variable
...
```

### JavaScript Objects

```markdown
...
75. Create a JS variable
76. Create a JS object
77. Create a person object (single line)
78. Create a person object (multiple lines)
79. Access object properties using `.property`
80. Access object properties using `[property]`
81. Access a function property as method
82. Access a function property as a property
...
```

### JS Functions

```markdown
...
83. A simple function
84. Function with an argument
85. Function with an argument 2
86. A function that returns a value
87. A function that converts Fahrenheit to Celsius
88. A function call `without()`
...
```

### JS Events

```markdown
...
89. An onclick event changes an HTML element
90. An onclick event changes its own element
91. An onclick event calls a function
...
```

### JS Strings

```markdown
...
92. String can be written with a single or double quotes.
93. Show some string examples
94. Backslash before quotes accepts quotes as quotes
95. Find the length of a string
96. You can break text string with a backslash.
97. You cannot break code with a backslash.
98. Find the position of the first occurrence of a text in a string - `indexOf()`
99. Search for a text in a string and return the text if found - `match()`
100. Replace characters in a string - `replace()`
101. Convert string to upper case - `toUpperCase()`
102. Convert string to lower case - `toLowerCase()`
103. Split a string into an array - `split()`
...
```

### JS Numbers

```markdown
...
104. Numbers can be written with or without decimals
105. Extra large or extra small numbers can be written with exponent notation
106. Numbers are considered accurate only to 15 digits
107. Floating point arithmetic is not always 100% accurate
108. But it helps to multiple and divide by 10
109. Adding two numbers results in a new number
110. Adding two numeric strings results in a concatenated string
111. Adding a numeric string and number also results in a concatenated string
112. Common mistake when adding string and numbers 1
113. Common mistake when adding strings and numbers 2
114. JavaScript will try to convert strings to numbers when dividing
115. JavaScript will try to convert strings when multiplying
116. JavaScript will NOT convert strings to numbers when adding
117. A number divided by a string is `NaN` (Not a Number)
118. The global JavaScript function is `Nan()` returns if a value is a number
119. Using NaN in a mathematical operation will always return NaN
120. Using NaN in a mathematical string operation will concatenate NaN
121. `NaN` (Not a Number) is number (Yes! typeof NaN returns number)
122. Infinity is returned if you clculate a number outside the largest possible number
123. Division by zero also generates Infinity
124. Infinity is a number (typeof Infinity returns number)
125. Constants, preceded by 0x, are interpreted as hexadecimal
126. The `toString()` method can output numbers as hex, octal, and binary
127. Numbers can be objects
128. Numbers can be objects 2
129. Numbers and objects cannot be safely compared
130. Objects and objects cannot be safely compared
...
```

### JS Number Methods

```markdown
...
132. The `toString()` method converts a number to a string
133. The `valueOf()` method returns a number wiht exponential notation
134. The `toExponential()` returns a number with exponential notation
135. The `toFixed()` method rounds a number to a number of digits
136. The `toPrecision()` method a number written with a specified length
137. The global method `Number()` converts variables to numbers
138. The global method `Number()` can even convert dates tu numbers
139. The global method `parseInt()` converts strings to numbers
140. The global methods `parseFloat()` converts strings tu numbers
141. `MAX_VALUE` returns the largest possible number in JavaScript
142. `MIN_VALUE` returns the smallest possible numbers in JavaScript
143. `POSITIVE_INFINITY` represents infinity
144. `POSITIVE_INFINITY` is retuned on overflow
145. `NEGATIVE_INFINITY` represents negative infinity
146. `NEGATIVE_INFINITY` is returned on overflow
147. `NaN` Represents "Not-a-Number"
148. Arithmetic performed on a string will result in `NaN`
149. Using a Number property on a variable will return undefined
...
```

### JS Maths

```markdown
...
150. `Math.PI` returns the value of PI
151. `Math.round(x)` returns the rounded value of x
152. `Math.pow(x, y)` returns the value of x to the power of y
153. `Math.sqrt(x)` returns the square root of x
154. `Math.abs(x)` returns the absolute (positive) value of x
155. `Math.ceil(x)` returns the value of x rounded up
156. `Math.floor(x)` returns the value of x rounded down
157. `Math.sin(x)` returns the sin of the angle x (given in radians)
158. `Math.cos(x)` returns the cosin of the angle x (given in radians)
159. `Math.max()` returns the number with the highest value from a list of arguments
160. `Math.min()` to return the number with the lowest value from a list of arguments
161. Converting Celsius to Farenheit
...
```

### JS Random

```markdown
...
162. Math.random() returns a number between 0 (included) and 1 (excluded)
163. How to return a random integer between 0 and 9 (both included)
164. How to return a random integer between 0 an 10 (both included)
165. How to return a random integer between 0 and 99 (both included)
166. How to return a random integer between 0 and 100 (both included)
167. How to return a random integer between 1 and 10 (both included)
168. How to return a random integer between 1 and 100 (both included)
169. How to return a random integer between x (included) and y (excluded)
170. How to return a random integer between x and y (both included)
...
```

### JS Dates

```markdown
...
171. Use `Date()` to display today's date and time
172. Use `getFullYear()` display the year
173. Use `getTime()` to calculate the number of milliseconds sonce 1970
174. Use `setFullYear()` to set a specific date
175. Use `toUTCString()` to convert todays's date (according to UTC) to string
176. Use `getDay()` to display the weekday as number
177. Use `getDay()` and and array to display the weekday as a name
178. Display a clock
...
```

### JS Arrays

```markdown
...
179. Create an array I
180. Create an array II
181. Access an array element
182. Change an array element
183. Access a full array
184. Find the length of an array
185. Loop through an array
186. Add an element to an array
187. Add undefined "holes" to an array
188. How to recognize an array I
189. How to recognize an array II
...
```

### JS Array Methods

```markdown
...
190. Add an element to an array
191. Remove the last element of an array - `pop()`
192. Join all elements of an array into a string - `join()`
193. Join two arrays - `concat()`
194. Join three arrays 0 `concat()`
195. Add an element to position 2 in a array - `splice()`
196. Convert an array to a string - `toString()`
197. Add new elements to the beginned of an array - `unshift()`
198. Remove the first element of an array - `shift()`
199. Select elements from an array - `slice()`
...
```

### JS Array Sort

```markdown
...
200. Sort an array in ascending order
201. Sort and array in descending order
202. Sort an array of numbers ascending
203. Sort an array of numbers descending
204. Sort numbers (alphabetically or numerically)
205. Sort array numbers in random order
206. Find the lowest number in an array
207. Find the highest number in an array
208. Find the lowest number in an array using `Math.min()`
209. Find the highest number in an array using `Math.max()`
210. Using a "homemade" `myArrayMin` method
211. Using a "homemade" `myArrayMax` method
212. Sort objects by numeric properties
213. Sort objects by string properties
...
```

### JS Array Iteration

```markdown
...
214.  `Array.forEach()`
215.  `Array.map()`
216.  `Array.filter()`
217.  `Array.reduce()`
218.  `Array.reduceRight()`
219.  `Array.every()`
220.  `Array.some()`
221.  `Array.indexOf()`
222.  `Array.lastIndexOf()`
223.  `Array.find()`
224.  `Array.findIndex()`
...
```

### JS Type Conversion

```markdown
...
225. Display typeof all variable types
226. Display the constructor of all varible types
227. Convert a number to a string using `String()`
228. Convert a number to a string using `toString()`
229. Find out if a variable is an array
230. Find out if a variable is a date
...
```

### JS Booleans

```markdown
...
231. Display the value of `Boolean(10 > 9)`
232. Display the value of `10 > 9`
233. Everything with a real value is true
234. The Boolean value of zero is false
235. The Boolean value of minus zero is false
236. The Boolean value of an empty string is false
237. The Boolean value of undefined is false
238. The Booleaan value of null is false
239. The Boolean value of false is false
240. The Boolean value of `NaN` is false
...
```

### JS Comparisons

```markdown
...
241. Assign 5 to x, and display the value of `(x == 8)`
242. Assgin 5 to x, and display the value of `(x == 5)`
243. Assign 5 to x, and display the value of `(x === 5)`
244. Assign 5 to x, and display the value of `(x === "5")`
245. Assign 5 to x, and dislpay the value of `(x != 8)`
246. Assign 5 to x, and display the value of `(x !== "5")`
247. Assign 5 to x, and display the value of `(x > 8)`
248. Assign 5 to x, and display the value of `(x < 8)`
249. Assign 5 to x, and display the value of `(x <= 8)`
250. Assign 5 to x, and display the value of `(x <= 8)`
...
```

### JS Conditionals

```markdown
...
251. The if statement
252. The else statement
253. The else if statement
254. Random link
255. Switch statement
...
```

### JS Loops

```markdown
...
256. For loop
257. Looping and Array
258. Looping through HTML headers
259. While loop
260. Do While loop
261. Break a loop
262. Break and continue a loop
263. Use a for...in statement to loop through the elements of an object
...
```

### JS Error Handling

```markdown
...
264. The try..catch statement
265. The try...catch statement with a confirm box
266. The onerror event
...
```

### JS Regular Expressions

```markdown
...
267. Search for an expression in a string
268. Search for an expression and replace it
...
```

### JS Objects 2

```markdown
...
269. Creating a JS variable
270. Creating a JS object
271. Creating a JS object (single line)
272. Creating a JS object (multiple lines)
273. Creating a JS object using new
274. Creating JS objects using a constructor
275. Creating built-in JS objects
276. The best way to create JS variables
277. JS objects are mutable
...
```

### JS Object Properties

```markdown
...
278. Accesing properties using `.property`
279. Accesing properties using `[property]`
280. Accesing properties using for in
281. Adding new properties to existing objects
282. Deleting properties from objects
...
```

### JSON Objects

```markdown
...
283. Accesing propeties using `.property`
284. Accesing properties `[property]`
285. Looping through properties
288. Looping through property values
289. Access nested JSON objects
290. Modify values using the dot notation
291. Modify values using the braket notation
292. Delete object properties
...
```

### JSON Arrays

```markdown
...
293. Accesing array values
294. Looping through an array using for-in
295. Looping through an array using for
296. Access nested JSON arrays
297. Modify array values
298. Delete array items
...
```

### JSON Parse

```markdown
...
299. Using JSON parse
300. Using JSON parse in AJAX example
301. Using JSON parse on an array
302. Parsing dates
303. Parsing dates suing reviver function
304. Parsing functions
...
```

### JSON Stringify

```markdown
...
305. Use JSON strigify
306. Using JSON strigify on an array
307. Stringify dates
308. Stringify functions
309. Stringify functions using toString() method
...
```

### JSON PHP

```markdown
...
310. Get JSON from a php file
311. Get JSON array from php
312. Get JSON from a database
313. Loop through the result from a database
314. Send JSON using POST method
...
```

### JSON HTML

```markdown
...
315. Make an HTML table based on JSON data
316. Make dynamic HTML Table
317. Make an HTML drop down list based on JSON data
...
```

### JSON JSONP

```markdown
...
318. Simple JSONP example
319. Create a dynamic script tag
320. JSONP example with dynamic result
321. JSONP example with a callback function
...
```

## JavaScript HTML DOM Examples

Examples of using JavaScript to access and manipulate DOM objects.

[Section URL](https://www.w3schools.com/js/js_dom_examples.asp)

### The Document Object

### The Anchors Collection

### The Links Collection

### The Forms Collection

### The Images Collection

### CSS Manipulation

### JavaScript HTML Input Examples

Examples of using JavaScript to access and manipulate HTML input objects.

[Section URL](https://www.w3schools.com/js/js_input_examples.asp)

## JavaScript HTML Objects Examples

Examples of using JavaScript to access and manipulate HTML objects.

[Section URL](https://www.w3schools.com/js/js_ex_dom.asp)

## JavaScript HTML DOM Events

Examples of using JavaScript to react to events

[Section URL](https://www.w3schools.com/js/js_events_examples.asp)

## JavaScript Browser Objects Examples

Examples of using JavaScript to access and manipulate the Browser objects.

[Section URL](https://www.w3schools.com/js/js_exercises.asp)

## JavaScript Exercises

[Exercises URL](https://www.w3schools.com/js/js_exercises.asp)

